{
 "build": {
   "image": "node:alpine",
   "command": "npm install && npm run build && npm run export && echo $SOME_ENV",
   "publicDir": "out",
   "baseDir": "/",
   "environment": {
     "SOME_ENV": "Build finished!"
   }
 }
}